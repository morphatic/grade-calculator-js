/**
 * @module grade-calculator.js
 *
 * This is a library of functions designed to be useful in a
 * variety of different types of grade calculations.
 */

/**
 * Calculates the average of an array of grades.
 *
 * @param   {number[]} grades An array of number grades
 * @returns {number}          The average of the grades
 */
function average (grades) {
  // WRITE CODE HERE TO MAKE THIS FUNCTION WORK!
  return 0
}

/**
 * Drops the lowest number and returns the pruned collection
 *
 * @param   {number[]} grades An array of number grades
 * @returns {number[]}        The pruned list of number grades
 */
function dropLowest (grades) {
  // WRITE CODE HERE TO MAKE THIS FUNCTION WORK!
  return []
}

/**
 * Takes an array of letter grades, and an object that provides
 * the relative weights of those letter grades in GPA format. It
 * calculates a GPA based on the number of grades and their weights.
 *
 * @param   {string[]} grades  An array of letter grades, e.g. A, B+, C, A-, etc.
 * @param   {object}   weights An object equating letter grades to their weights
 * @returns {number}           The calculated GPA
 */
function calculateGPA (grades, weights) {
  // WRITE CODE HERE TO MAKE THIS FUNCTION WORK!
  return 0
}

// export the functions for use in other files
module.exports = {
  average,
  dropLowest,
  calculateGPA,
}
